import { Users } from 'lucide-react';
import { PlaceholderPage } from '../../components/Shared/PlaceholderPage';

export const ClientsPage = () => {
  return (
    <PlaceholderPage
      title="Clients"
      icon={Users}
      description="Manage your client relationships, track engagements, and maintain detailed contact information."
      color="tg-primary"
    />
  );
};
