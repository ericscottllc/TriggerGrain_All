import { Settings } from 'lucide-react';
import { PlaceholderPage } from '../../components/Shared/PlaceholderPage';

export const SettingsPage = () => {
  return (
    <PlaceholderPage
      title="Settings"
      icon={Settings}
      description="Manage your account settings, preferences, and application configuration."
      color="tg-grey"
    />
  );
};
