import { FileText } from 'lucide-react';
import { PlaceholderPage } from '../../components/Shared/PlaceholderPage';

export const OnePagerPage = () => {
  return (
    <PlaceholderPage
      title="One Pager"
      icon={FileText}
      description="Generate comprehensive market summaries and grain price reports in a single, shareable document."
      color="tg-coral"
    />
  );
};
